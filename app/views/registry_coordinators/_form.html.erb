<%= form_for(@registry_coordinator) do |f| %>
  <% if @registry_coordinator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registry_coordinator.errors.count, "error") %> prohibited this registry_coordinator from being saved:</h2>

      <ul>
      <% @registry_coordinator.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :location %><br>
      <%= f.text_field :location %>
    </div>
    <div class="field">
      <%= f.label :state %><br>
      <%= f.text_field :state %>
    </div>
    <div class="field">
      <%= f.label :coordinator_id %><br>
      <%= f.select :coordinator_id, @coordinator_ids  %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
